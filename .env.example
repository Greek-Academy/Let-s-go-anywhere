# Build/development configuration only. Both values are validated; other sources are unsupported.
DRIVEPLUS_RELEASE_CHANNEL=preview
DRIVEPLUS_CONTENT_SOURCE=sample
# Do not put API keys or private information in client-side environment variables.
